// lib/screens/student/create_report_screen.dart
import 'package:flutter/material.dart';

class CreateReportScreen extends StatelessWidget {
  const CreateReportScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('إنشاء تقرير جديد'),
      ),
      body: const Center(
        child: Text('شاشة إنشاء التقرير - جاري التطوير'),
      ),
    );
  }
}
